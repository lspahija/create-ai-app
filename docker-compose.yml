services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - app-data:/app/data
    environment:
      - CLAUDE_CODE_OAUTH_TOKEN=${CLAUDE_CODE_OAUTH_TOKEN:-}
      - AUTH_PASSWORD=${AUTH_PASSWORD:-}
    restart: unless-stopped

volumes:
  app-data:
